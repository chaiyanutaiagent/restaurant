# Project Instructions for Codex

Project path:
`/Users/user/Projects/restaurant`

This project must use only this GitHub repository:

`https://github.com/chaiyanutaiagent/restaurant`

Rules:
- Treat `origin` as the only active remote.
- Do not push this project to `chaiyanutaiagent/erp-pos-run`.
- Keep the ERP-POS source repository unchanged.
- Do not change Git remotes unless explicitly instructed.
- Before any push, verify:

```bash
git remote -v
gh repo view
git status
git branch -vv
```
